name: Tests
on: 
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    environment: main
    steps:
      - uses: actions/checkout@v2
      - name: Install Modules
        run: npm i
      - name: Run Tests
        run: npm run test
